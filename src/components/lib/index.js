import { HomeWidget, PurchasesWidget, ShopWidget, ChatWidget, CheckoutWidget} from './widgets';
import  Gate from './gate';
import UserComponent from './userComponent';

export { 
    HomeWidget, 
    PurchasesWidget, 
    ShopWidget, 
    ChatWidget,
    CheckoutWidget,
    UserComponent,
    Gate
}